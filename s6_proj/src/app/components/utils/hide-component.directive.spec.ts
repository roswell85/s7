import { HideComponentDirective } from './hide-component.directive';

describe('HideComponentDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new HideComponentDirective();
    expect(directive).toBeTruthy();
  });
});
